# How to run

 - Install in VSCode Live Server plugin.

 - When you have installed. Click on Live Server button to launch server.

 - The browser automatically opens the web application in the port 5500.

- To check test cases, update  `index.js` file. In the lines 86 to 92 uncomment one `drawContainer` call. Save the file and automatically the browser updates.
